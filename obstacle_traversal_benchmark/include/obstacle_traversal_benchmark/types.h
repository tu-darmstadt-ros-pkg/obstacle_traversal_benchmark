#ifndef OBSTACLE_TRAVERSAL_BENCHMARK_TYPES_H
#define OBSTACLE_TRAVERSAL_BENCHMARK_TYPES_H

#include <string>
#include <unordered_map>

namespace obstacle_traversal_benchmark {
  typedef std::unordered_map<std::string, double> JointPositionMap;
}

#endif  // OBSTACLE_TRAVERSAL_BENCHMARK_TYPES_H
